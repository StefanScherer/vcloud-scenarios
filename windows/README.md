# windows
Each of these sub directories contain scenarios for windows guests to be used with vagrant-vcloud plugin.

It is used to test the new Vagrant 1.6 features like the winrm communicator on
top of the still needed ssh port (for rsync) and the `vagrant rdp` feature to
open a remote desktop connection.
